---
- name: "{{ role_name }} | {{ ansible_distribution }} | Uninstall conflicting packages"
  ansible.builtin.dnf:
    name:
      - sway-config-minimal
      - sway-config-upstream
    state: absent
  become: true

- name: "{{ role_name }} | {{ ansible_distribution }} | Install"
  ansible.builtin.dnf:
    name:
      - "@sway-desktop-environment"
      - desktop-backgrounds-compat
      - kanshi
      - rofi-themes
      - rofi-wayland
      - sway-config-fedora
      - sway-systemd
    state: present
  become: true
