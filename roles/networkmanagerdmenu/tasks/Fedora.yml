---
- name: "{{ role_name }} | {{ ansible_distribution }} | Install"
  ansible.builtin.dnf:
    name:
      - libnotify
      - network-manager-applet
      - pinentry-gnome3
      - python3-gobject
      - rofi-wayland
    state: present
  become: true
