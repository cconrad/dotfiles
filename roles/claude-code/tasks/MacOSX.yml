---
- name: "Claude Code | macOS | Install dependency"
  community.general.homebrew:
    name: node
    state: present

- name: "Claude Code | macOS | Install"
  ansible.builtin.include_tasks: npm.yml
